/*

Ejercicios Prácticos de Pilas en Java 
Autor: Kevin Eduardo Cuevas Castillo 

*/

// aqui uso una pila para invertir una palabra

import java.util.Stack;
import java.util.Scanner;

public class InvertirPalabra {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        System.out.print("Ingrese una palabra: ");
        String palabra = sc.nextLine();

        Stack<Character> pila = new Stack<>();

        // meto cada letra a la pila
        for (char c : palabra.toCharArray()) {
            pila.push(c);
        }

        // aqui voy sacando para mostrar la palabra al reves
        System.out.print("Palabra invertida: ");
        while (!pila.isEmpty()) {
            System.out.print(pila.pop());
        }
        System.out.println();
    }
}
